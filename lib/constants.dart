import 'dart:io';

import 'package:flutter/widgets.dart';

class Constants {
  static const Radius radius = Radius.circular(20);
  static const double topPadding = 130.0;
  static double homeTopPadding = 90.0;
  static const double bottomPadding = 110.0;
}
